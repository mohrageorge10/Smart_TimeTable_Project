import 'package:flutter/material.dart';

class AppMode {
  final String title;
  final IconData icon;
  final Color color;

  AppMode({
    required this.title,
    required this.icon,
    required this.color,
  });
}